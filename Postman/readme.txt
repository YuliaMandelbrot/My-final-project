Создать в POSTMAN новую коллекцию на основе свагера : Swagger UI 

Минимум 4 запроса (последовательность не важна) (и какие запросы - также не важны, но хотелось бы стандартные: GET, POST, PUT, DELETE)

Покрытие минимальными проверками тестами (по 3 - 4 на каждый API-запрос)

Задание со звездочкой : Уметь запускать коллекцию с командной строки (придется гуглить)
